@model BikeCommunitySite.Models.GooglePlaceModel.Result

<div class="row">
    <div class="box">
        <div class="col-lg-12">
            <hr>
            <h2 class="intro-text text-center">
                @Model.name
            </h2>
            <hr>
            @*<img class="img-responsive img-border img-left" src="" alt="">*@
            <hr class="visible-xs">
            <p>Description:</p>
            <p>@Model.vicinity</p>
        </div>
        <div>
            @*<a href="@Url.Action("Destinations", "Home")" class="btn btn-primary" role="button">Back to List</a>*@
        </div>
    </div>
</div>
